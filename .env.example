# Android Debug MCP Server Configuration

# ADB/Fastboot binary paths (optional, will use system PATH if not specified)
ADB_PATH=/path/to/platform-tools/adb
FASTBOOT_PATH=/path/to/platform-tools/fastboot

# Default timeout for commands (milliseconds)
COMMAND_TIMEOUT=30000

# Maximum log lines to return
MAX_LOG_LINES=1000

# Character limit for responses
CHARACTER_LIMIT=25000
